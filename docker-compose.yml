version: '3.8'

services:
  app:
    build: .
    container_name: laravel_app_ventanilla
    ports:
      - "8080:80"
    volumes:
      - .:/var/www/html
    networks:
      - laravel

networks:
  laravel:
    driver: bridge